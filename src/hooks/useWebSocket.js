export function useWebSocket() {
  // Spec contract:
  // - Optional ESP32 input bridge.
  // - Mirror the same actions available from on-screen controls.
  // - Silent fallback to standalone UI when socket is unavailable.
  return null;
}
