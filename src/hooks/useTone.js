export function useTone() {
  // Spec contract:
  // - Own Tone.Transport lifecycle and per-layer synth instances.
  // - Trigger live notes for SoftPot gestures.
  // - Expose filter/reverb/layer-fx update controls.
  // - Schedule loop playback from committed recordings.
  return {};
}
