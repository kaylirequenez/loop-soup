// Spec contract:
// - Single source of truth for transport/session state and per-layer state.
// - Must include selectedLayer, currentView, picker/sample state, repeats (max 4),
//   octave, layer fx params, recording buffers, and loop metadata.
// - Step 2 will replace this placeholder with a full zustand store.
export const useAppStore = () => ({});
