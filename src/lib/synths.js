export function createSynth() {
  // Spec contract:
  // - Build per-layer Tone.js instrument + filter/reverb/layer-fx chain.
  // - Return a uniform interface: triggerAttack, triggerRelease,
  //   triggerAttackRelease, filter, reverb, layerFx.update, dispose.
  return null;
}
