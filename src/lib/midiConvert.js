export function contourToMidi() {
  // Spec contract:
  // - Convert Essentia pitch contour + confidence arrays into note events.
  // - Quantize start/duration to rhythmic grid (16th-note baseline).
  return [];
}
